**Summary**: A class of which only a single instance can exist

The **Singleton Pattern** is a creational design pattern that ensures a class has only one instance and provides a global access point to it.
